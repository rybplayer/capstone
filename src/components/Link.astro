---
import { cn, withBase } from '@/lib/utils'

interface Props {
  href: string
  external?: boolean
  class?: string
  underline?: boolean
  [key: string]: any
}

const { href, external, class: className, underline, ...rest } = Astro.props
const resolvedHref = !external && href ? withBase(href) : href
---

<a
  href={resolvedHref}
  target={external ? '_blank' : '_self'}
  class={cn(
    'transition-colors duration-300 ease-in-out hyphens-auto inline-block',
    underline &&
      'underline decoration-muted-foreground underline-offset-[3px] hover:decoration-foreground hover:text-foreground',
    className,
  )}
  {...rest}
>
  <slot />
</a>
